<%= form_tag search_results_path, method: 'get' do %>

  <div class="form-group">
   
    <div class="form-group"> <%= text_field_tag :search_keywords, nil, placeholder: "headline" %>
     <%= submit_tag "Search", class: "btn btn-primary" %>
  </div>
  
<% end %>
